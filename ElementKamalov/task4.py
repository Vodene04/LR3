class ElementKamalov():
        def __init__(self, name, symbol, number):
                self.name = name
                self.symbol = symbol
                self.number = number

element = ElementKamalov('Кислород', 'O', 8)